{
  "name": "rf-aggregator-edge",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply rf_aggregator --local",
    "db:migrate": "wrangler d1 migrations apply rf_aggregator --remote"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
